export const docCache = new Map();
export const unitCache = new Map();

export const state = {
    isProcessing: false,
    basePath: '',
    execAcode: '',
    cachedAuthToken: '',
    unitsPrimed: false
};
